-- NFC functionality --
pip install pyscard

--CSV functionality--
pip install pandas

--AES/RSA/AES and RSA--
pip install pycryptodome

--ECC--
pip install cryptography

--HILLCIPHER--
pip install numpy

Notes:
- It's best if each algorithm has their own directory in your project, since the section of the codes that reads data from the CSV will always assume the CSV is one level up. Additionally it helps in organization of pem and bin files.